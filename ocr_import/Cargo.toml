[package]
name = "ocr_import"
version = "0.1.0"
edition = "2021"

[dependencies]
pentacam_types = { path = "../pentacam_types" }
image = "0.25"
regex = "1"
once_cell = "1"

# Heavy deps — enable when building with OCR support.
# On systems without libssl-dev, build with default features only.
# oar-ocr = "0.6"
# pdfium-render = "0.8"
# imageproc = "0.25"
# nalgebra = "0.33"
